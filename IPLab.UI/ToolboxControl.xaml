<UserControl x:Class="IPLab.UI.ToolboxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:IPLab.UI.ViewModels">

    <Border Background="#252526" BorderBrush="#3F3F46" BorderThickness="0 0 1 0">
        <DockPanel>

            <!-- Header -->
            <Border DockPanel.Dock="Top" Background="#2D2D30" Padding="8 5">
                <TextBlock Text="Operators" FontWeight="SemiBold" Foreground="White" />
            </Border>

            <!-- Grouped operator list -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ToolboxGroupViewModel}">
                            <Expander IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                                      Background="Transparent"
                                      Foreground="#DCDCDC"
                                      BorderThickness="0"
                                      Margin="0 2 0 0">
                                <Expander.Header>
                                    <TextBlock Text="{Binding Category}"
                                               FontWeight="SemiBold"
                                               Foreground="#AAAAAA"
                                               FontSize="11" />
                                </Expander.Header>

                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:ToolboxItemViewModel}">
                                            <Grid Margin="4 1">
                                                <Grid.InputBindings>
                                                    <MouseBinding Gesture="LeftDoubleClick"
                                                                  Command="{Binding AddCommand}" />
                                                </Grid.InputBindings>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="22" />
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding TypeName}"
                                                           Foreground="#DCDCDC"
                                                           VerticalAlignment="Center"
                                                           FontSize="12"
                                                           Margin="6 2 0 2" />

                                                <Button Grid.Column="1"
                                                        Content="+"
                                                        Command="{Binding AddCommand}"
                                                        Width="20" Height="20"
                                                        Padding="0"
                                                        Background="Transparent"
                                                        BorderBrush="#3F3F46"
                                                        BorderThickness="1"
                                                        Foreground="#007ACC"
                                                        FontWeight="Bold"
                                                        FontSize="14"
                                                        Cursor="Hand"
                                                        ToolTip="Add to graph" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </DockPanel>
    </Border>
</UserControl>
